// ============================================================
// src/App.jsx
// Root component — renders the route tree
// ============================================================

import AppRoutes from './routes/AppRoutes';

const App = () => {
  return <AppRoutes />;
};

export default App;